import { ProfessorImagePipe } from './professor-image.pipe';

describe('ProfessorImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfessorImagePipe();
    expect(pipe).toBeTruthy();
  });
});
