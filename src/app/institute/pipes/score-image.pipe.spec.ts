import { ScoreImagePipe } from './score-image.pipe';

describe('ScoreImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreImagePipe();
    expect(pipe).toBeTruthy();
  });
});
