import { StudentImagePipe } from './student-image.pipe';

describe('StudentImagePipe', () => {
  it('create an instance', () => {
    const pipe = new StudentImagePipe();
    expect(pipe).toBeTruthy();
  });
});
